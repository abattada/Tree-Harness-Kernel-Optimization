# install torch first with the CUDA build matching your driver, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.7
triton>=3.3
openai>=1.60
pydantic>=2.6
pyyaml>=6.0
pandas>=2.0
matplotlib>=3.8
tabulate>=0.9
